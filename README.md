# Linux-System-Monitor-Tool-
A terminal-based Linux System Monitor built in C++ using Ncurses. Displays real-time CPU and memory usage, running processes, uptime, and allows process control — all from the command line.
